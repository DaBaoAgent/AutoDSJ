# Optional local CAM++ speaker evidence.  Install only when `dy voices` is used.
torch>=2.2,<3
torchaudio>=2.2,<3
numpy>=1.26,<2
scipy>=1.11,<2
modelscope>=1.28,<2
# Install SpeakerLab separately without its training/visualization dependency
# tree: `uv pip install --python .venv/Scripts/python.exe --no-deps speakerlab==0.0.6`.
