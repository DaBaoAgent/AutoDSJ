# DY 工作流 — 纯后端运行依赖（Python 3.11+）
# 视觉识别 + 百炼配音
dashscope>=1.26.2,<2
# 配置模型
pydantic>=2.8,<3
# API Key 本地加密存储
cryptography>=42.0,<46

# 说明：
# - FFmpeg / FFprobe 由 tools/ffmpeg/bin 内置提供，无需 pip 安装。
#   不属于本主环境依赖；默认百炼克隆音色链路不涉及。
